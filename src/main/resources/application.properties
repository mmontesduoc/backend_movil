spring.application.name=levelupgamer

# Oracle local connection (SID: XE)
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:XE
spring.datasource.username=C##PROYECTO_GAMER
spring.datasource.password=Sth_s0p0rte
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver
# Use a generic Oracle dialect to avoid ClassNotFound for specific dialect classes
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=update
server.port=8080

# JWT
jwt.secret=dev-long-secret-please-change-in-production-0123456789abcdef
jwt.expiration=3600000

spring.sql.init.mode=never

# Disable DevTools automatic restart to avoid unexpected restarts during testing
spring.devtools.restart.enabled=false

# Prefixing moved to controller-level @RequestMapping annotations
# server.servlet.context-path removed to keep mappings explicit in controllers